BUNDLE_NAME="Three-Body Problem"
EXECUTABLE="ThreeBodySaver"
MODULES="SaverKit ThreeBodyCore ThreeBodyRender ThreeBodySaver"
FRAMEWORKS="ScreenSaver AppKit"
DEPLOYMENT_TARGET="14.0"
C_SOURCES=""
C_INCLUDE=""
APP_PRODUCT="ThreeBodyApp"
EXTRA_RESOURCES=""
ZIP_PREFIX="ThreeBodyProblem"
# Core Graphics draws into the view's backing store, so a captured frame is the
# real thing and worth asserting on.
VERIFY_RENDER="cpu"
